#include <stdio.h>
#define LOWER 0
#define UPPER 300
#define STEP 20
int main (){
    float F , celcius;

    for(F = LOWER; F <= UPPER; F += STEP)
    {
        celcius = (5.0/9.0) * (F -32);
        printf("%3.2f %5.3f\n", F, celcius);
    }
    return 0;
}